import { ClusterEfficiency } from './cluster-efficiency';

describe('ClusterEfficiency', () => {
  it('should create an instance', () => {
    expect(new ClusterEfficiency()).toBeTruthy();
  });
});
